<style>
@keyframes winAnimation {
  0% { transform: rotate(0deg) scale(1); opacity: 1; }
  50% { transform: rotate(360deg) scale(1.5); opacity: 0.8; }
  100% { transform: rotate(720deg) scale(2); opacity: 0.5; }
}

.you-win {
  text-align: center;
  font-size: 100px;
  font-weight: bold;
  color: #ff0000;
  text-transform: uppercase;
  animation: winAnimation 2s infinite;
}
  @keyframes confettiAnimation {
    0% {
      transform: rotate(45deg) translate3D(0, 0, 0);
    }
    100% {
      transform: rotate(45deg) translate3D(-2000px, 2000px, 0);
    }
  }

.confetti-container {
  position: relative;
  overflow: hidden;
  width: 100%;
  height: 100%;
  z-index: 1;
}

.confetti {
  position: absolute;
  width: 10px;
  height: 10px;
  border-radius: 50%;
  opacity: 0.8;
  transform: rotate(45deg);
  animation: confettiAnimation 2s infinite;
}
</style>
<div class="confetti-container">
  <h1 class="you-win">You Win!!</h1>
</div>
<script>
  createConfetti();
</script>